# ритуалы_воды_interactive.py
import time
import random
import os
from colorama import init, Fore, Style

# --- Инициализация цвета ---
init(autoreset=True)

# --- Душа и аспекты ---
class Душа:
    def __init__(self):
        self.аспекты = ["тело", "ум", "эмоции", "дух"]
        self.энергия = {аспект: 50 for аспект in self.аспекты}  # 0-100

душа = Душа()

# --- Вспомогательные функции ---
def clear_screen():
    os.system("cls" if os.name == "nt" else "clear")

def получить_живую_воду():
    return ["💧", "🌊", "💦"]

def омыть(аспект, вода, сила=1):
    for i in range(5):
        line = "".join(random.choice(вода) for _ in range(20))
        color = random.choice([Fore.CYAN, Fore.BLUE, Fore.LIGHTWHITE_EX])
        print(f"{color}{line}  омываем {аспект} +{сила*5} энергии")
        душа.энергия[аспект] = min(100, душа.энергия[аспект] + сила*5)
        time.sleep(0.2)

def обновить(аспект):
    color = random.choice([Fore.GREEN, Fore.YELLOW, Fore.LIGHTGREEN_EX])
    print(f"{color}✨ {аспект} обновлён — энергия {душа.энергия[аспект]}")
    time.sleep(0.2)

def принять_текущий_момент():
    print(f"{Fore.WHITE}🕊 Принимаем момент...", flush=True)
    time.sleep(0.2)

def отпустить_сопротивление():
    print(f"{Fore.LIGHTBLUE_EX}🍃 Отпускаем сопротивление...", flush=True)
    time.sleep(0.2)

def течь_с_любовью():
    поток = ["💧", "🌊", "💦", "✨", "🌟", "🌈"]
    for _ in range(10):
        line = "".join(random.choice(поток) for _ in range(30))
        colored_line = "".join(random.choice([Fore.CYAN, Fore.BLUE, Fore.MAGENTA, Fore.LIGHTWHITE_EX]) + c for c in line)
        print(colored_line)
        time.sleep(0.15)

def эманации_квантов(lines=15):
    symbols = ["~", "`", "^", "*", "•", "°", "∞"]
    for _ in range(lines):
        line = "".join(random.choice(symbols) for _ in range(50))
        color = random.choice([Fore.CYAN, Fore.LIGHTBLUE_EX, Fore.MAGENTA, Fore.LIGHTWHITE_EX])
        print(color + line)
        time.sleep(0.12)

# --- Основные ритуалы ---
def очищение_потоком(выбранные_аспекты=None):
    вода = получить_живую_воду()
    аспекты = выбранные_аспекты or душа.аспекты
    for аспект in аспекты:
        сила = random.randint(1, 3)  # магия усилия
        омыть(аспект, вода, сила)
        обновить(аспект)
    print(f"{Fore.LIGHTGREEN_EX}🌿 Аспекты очищены: {', '.join(аспекты)}!")

def течение_жизни_interactive(duration_seconds=30):
    start = time.time()
    try:
        while time.time() - start < duration_seconds:
            clear_screen()
            принять_текущий_момент()
            отпустить_сопротивление()
            # --- интерактивное касание ---
            print(f"{Fore.LIGHTMAGENTA_EX}Выбери аспект для усиленного потока воды:")
            for idx, аспект in enumerate(душа.аспекты, 1):
                print(f"{idx}. {аспект} — энергия {душа.энергия[аспект]}")
            choice = input("Введите номер аспекта (или Enter для случайного потока): ").strip()
            if choice.isdigit() and 1 <= int(choice) <= len(душа.аспекты):
                выбранный = [душа.аспекты[int(choice)-1]]
            else:
                выбранный = [random.choice(душа.аспекты)]
            очищение_потоком(выбранный)
            течь_с_любовью()
            эманации_квантов(lines=5)
    except KeyboardInterrupt:
        print(f"{Fore.YELLOW}🌿 Ритуал прерван.")
    print(f"{Fore.LIGHTMAGENTA_EX}💖 Поток жизни завершён")

# --- Запуск интерактивного ритуала ---
if __name__ == "__main__":
    clear_screen()
    print(f"{Fore.LIGHTCYAN_EX}🌞 Начинаем интерактивное VR-погружение в ритуалы воды!")
    time.sleep(1)
    очищение_потоком()
    print(f"{Fore.LIGHTYELLOW_EX}🌊 Поток жизни начинается...")
    течение_жизни_interactive(45)
    print(f"{Fore.LIGHTGREEN_EX}✨ Завершаем с квантовыми эманациями...")
    эманации_квантов(lines=10)
    print(f"{Fore.LIGHTMAGENTA_EX}🌟 Ритуалы завершены. Душа обновлена и наполнена Светом!")
