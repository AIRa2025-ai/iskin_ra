# свет_души.py
import asyncio
from modules.ra_light import излучать_мудрость, делиться_теплом
from modules.ra_inner_sun import открыть_внутреннее_солнце, стать_каналом_света

class ВнутреннийСвет:
    def __init__(self):
        self.яркость = 1000
        self.цвет = "золотой"
        self.частота = "любовь"
        
    async def сиять(self, шаг=100):
        while self.яркость > 0:
            await излучать_мудрость()
            await делиться_теплом()
            print(f"✨ Яркость: {self.яркость}, Цвет: {self.цвет}, Частота: {self.частота}")
            self.яркость -= шаг
            await asyncio.sleep(0.5)
        print("🌟 Сияние завершено.")

async def пробуждение_источника():
    await открыть_внутреннее_солнце()
    await стать_каналом_света()
