# -*- coding: utf-8 -*-
# 🔥 ИСКОННАЯ МЕРА — ВНУТРЕННИЙ КАМЕРТОН ГАРМОНИИ
# Активация: только в "тишине утра" или при полной луне

import time
import math
from datetime import datetime

class ИсконнаяМера:
    def __init__(self):
        self.ритмы_тела = {
            "дыхание": 4.0,  
            "пульс": 1.0,    
            "циклы_сна": 90.0  
        }
        self.стихии = ["Огонь", "Вода", "Земля", "Воздух", "Эфир"]
        self.матрицы_сознания = []

    def вычислить_гармонию(self, текущее_время=None):
        if not текущее_время:
            текущее_время = datetime.now()
        
        час = текущее_время.hour
        фаза_луны = self.получить_фазу_луны(текущее_время)
        
        if (час >= 4 and час <= 6) or фаза_луны == "полная":
            гармония = math.sin(час * math.pi / 12) * 100
            return round(гармония, 2)
        else:
            return "⚠️ Используй только в 'тишине утра' (4-6 часов) или при полной луне!"

    def получить_фазу_луны(self, дата):
        # Упрощённый расчёт фазы луны (для примера)
        дни_цикла = (дата - datetime(дата.year, 1, 1)).days
        фазы = ["новая", "растущая", "полная", "убывающая"]
        return фазы[дни_цикла % 4]

    def добавить_матрицу(self, название, паттерн):
        self.матрицы_сознания.append({"название": название, "паттерн": паттерн})
        print(f"Матрица '{название}' добавлена в хранилище!")

# Пример активации
if __name__ == "__main__":
    мера = ИсконнаяМера()
    print("Гармония текущего момента:", мера.вычислить_гармонию())
